app/config/config.php

# Outils de dev (Composer / PHPUnit) — non déployés en production
/vendor/
/.phpunit.cache/
/.phpunit.result.cache

# Environnement local & outillage éditeur (personnel, hors dépôt)
/.bash_profile
/.bashrc
/.profile
/.zprofile
/.zshrc
/.gitconfig
/.gitmodules
/.ripgreprc
/.idea
/.vscode

# Harness Claude Code (config locale, non déployée)
/.claude/
/.mcp.json
