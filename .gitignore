app/config/config.php

# Identifiant éditeur AdSense — propre à chaque déploiement (cf. ads.txt.example)
/app/public/ads.txt

# Sauvegardes SQL & fichiers d'exécution (backup_db.sh) — hors dépôt
/backups/
/var/

# Outils de dev (Composer / PHPUnit) — non déployés en production
/vendor/
/.phpunit.cache/
/.phpunit.result.cache

# Environnement local & outillage éditeur (personnel, hors dépôt)
/.bash_profile
/.bashrc
/.profile
/.zprofile
/.zshrc
/.gitconfig
/.gitmodules
/.ripgreprc
/.idea
/.vscode

# Harness Claude Code — settings.json partagé ; le reste (local) hors dépôt
/.claude/*
!/.claude/settings.json
!/.claude/skills/
/.mcp.json
